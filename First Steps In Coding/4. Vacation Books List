using System;

class HelloCsharp
{
    static void Main()
    {
        int numberOfPages = int.Parse(Console.ReadLine());
        double pages = double.Parse(Console.ReadLine());
        int numberOfDays = int.Parse(Console.ReadLine());

        Console.WriteLine(Math.Round( numberOfPages/pages/numberOfDays));
    }
}
